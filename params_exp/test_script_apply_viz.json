{
	"situation_definition_fname" : "situation_definitions/dogwalking.json",
			
	"situate_parameterizations_fnames" : [
				"params_run/homebrew_rcnn_priming.json"
				//"params_run/faster_rcnn_priming.json",
				//"params_run/uniform.json"
			],	

	"experiment_settings" : {

			"description" : "situate update testing, viz",

			// full path, or relative to base_image_directories
			"directory_train" : "DogWalking_PortlandSimple_train/",
			"directory_test"  : "DogWalking_PortlandSimple_train/",

			"use_parallel" : 0, 
			
			"run_analysis_after_completion"	: 0,

			"use_visualizer" : 1,

			// additional visualization options
		 	"viz_options" : {
		 		"on_iteration_mod"    : 1,
		 		"on_workspace_change" : 0,
		 		"on_end"              : 1,
		 		"start_paused"        : 1
	 		},

			// training / testing image lists
			// empty should use all files in specified folders
			"training_testing_split_directory" : "data_splits/dogwalking_validation/",

			// number of folds
			// 	if image lists were provided
			// 		if this is empty, then all of them will be used.
			//  	if this is not empty, it will be a limit on how many to use.
			//	if image list is not provided, this will be used when generating 
			//		training/testing image lists
			"num_folds" : 1,
			"specific_folds" : [1],
			"max_testing_images" : 1,
			
			// if specified, this will be used to seed the RNG during testing.
			// otherwise, each parameterization will get a different time-based seed
			"testing_seed"  : ""

	}

}